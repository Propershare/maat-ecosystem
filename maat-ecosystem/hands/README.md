# Hands — action, tools, compositions

This folder holds **apps**, **skills**, **tool definitions**, and **client-side MCP adapters** (e.g. `mcps/mcp.py`) — how a body *uses* tools.

**Runnable MCP server processes** (Ka discovery, Tehuti Core, Maat Memory, pipeline, …) live next to the ecosystem root, not inside `hands/`:

- **`../mcp-servers/`** — canonical tree; lab-wide symlink `mcp-servers` → this path.

See **`../mcp-servers/README.md`**.
