# Maat ecosystem in Tehuti Lab monorepo

This directory is the **canonical Ka-body** for the MAAT Ecosystem in the Tehuti Lab workspace.

- **Previous clone:** `~/maat-ecosystem` on the same machine may still exist as a backup or for Git remotes. Prefer editing **this** tree when working inside `~/.n8n` (or your future renamed lab root).
- **DNA:** Read `MANIFEST.ka` first — it maps organs (`soul`, `brain`, `memory`, `hands`, `senses`, `voice`, `ka`, `skeleton`, `blood`).
- **Full overview:** `README.md` in this folder.

To add Git history here (optional):

```bash
cd maat-ecosystem
git init
git remote add origin <your-remote-url>
# or: copy .git from ~/maat-ecosystem if you intend to keep that repo identity
```
