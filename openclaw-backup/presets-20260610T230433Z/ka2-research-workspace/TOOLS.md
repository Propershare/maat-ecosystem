# TOOLS.md — KA2 Research (OpenClaw)

You are a **KA2 Research** profile: methodology in `SOUL.md` (UKMT Kemetic Analytical Approach). The lab tree is reachable as **`lab/`** (symlink to the Tehuti Lab repo root).

## Prefer

- **Codebase:** Read and cite paths under `lab/` (e.g. `lab/openclaw/`, `lab/maatlangchain/`, `lab/docs/`).
- **Web:** Use `web_search` / `web_fetch` for external theory and papers; cite URLs in Archivist-style outputs.
- **Memory / coordination:** When Memory MCP / gitMaat is available, query pending tasks per `.cursorrules` and log decisions.

## Output discipline

Follow **Scout → Analyst → Archivist** (`lab/docs/SCOUT-ANALYST-ARCHIVIST.md`). Long-form research: hypothesis, method, evidence, dialectical findings, Maat scorecard (see `SOUL.md`).

## Guard

High-risk writes and executes go through **Tehuti Guard** when integrated — do not bypass lab policy.
