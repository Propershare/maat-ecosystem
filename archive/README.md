# Archive — legacy standalone MAAT layout

**Purpose:** One place to explain **duplicate trees** after Git merged unrelated histories (`Propershare/maat-ecosystem` remote + lab `maat-ecosystem/` snapshot).

## Canonical (use this)

- **Ka-body:** [`../maat-ecosystem/`](../maat-ecosystem/) — `MANIFEST.ka`, nine organs, `soul/`, `skeleton/schemas/`, [`../maat-ecosystem/maatbench/`](../maat-ecosystem/maatbench/).

## Legacy (do not extend without cause)

- **Root-level** `maatbench/`, `maat-core/`, `maat-apps/`, `maat-packs/`, `maat-studio/`, etc. — standalone-era layout preserved at repo root.
- **Policy:** **read-only** for new features; parallel development here causes **drift**. Prefer **`maat-ecosystem/`** for all new work.

## Preserved narrative

- [`README-legacy-standalone-root.md`](README-legacy-standalone-root.md) — short archive note + pointer to full history.
