# MaatLangChain Requirements
# Following Maat principles: Truth (verified), Balance (minimal dependencies), Order (organized)

# Core LangChain (for migration - will be replaced)
langchain>=1.2.0
langchain-community>=0.4.1
langchain-ollama>=0.1.0
langchain-huggingface>=1.2.0

# LangGraph for agent workflows
langgraph>=0.2.0
langgraph-checkpoint>=0.2.0

# Document Processing
pypdf2>=3.0.1
pdf2image>=1.16.0
Pillow>=10.0.0

# Embeddings
sentence-transformers>=2.2.2
torch>=2.0.0

# PostgreSQL/pgvector (Production vector store)
psycopg2-binary>=2.9.7
pgvector>=0.2.3

# API Server (for OpenWebUI integration)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Utilities
tqdm>=4.66.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.28.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0  # For FastAPI TestClient

# Note: Following Maat principles
# - Minimal dependencies (Balance)
# - Production-ready (Truth)
# - Documented (Order)

